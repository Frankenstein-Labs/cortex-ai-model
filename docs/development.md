# Development

Use Python 3.10+, install editable dependencies, run `pytest`, and keep model weights outside the repository. Use atomic commits and document upstream source, version, license, and modifications. CI intentionally uses fixtures and never downloads full model weights.
