# Training

Inference, fine-tuning, continued pretraining, and evaluation are separate operations. SFT, LoRA, and QLoRA require an explicit backend, dataset approval, resource estimate, checkpoint/version policy, and an explicit user command. No costly training is launched automatically.
